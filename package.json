{
  "name": "mpl-id-simulator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "clean": "rm -rf dist",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "dotenv": "^17.2.3",
    "firebase": "^10.0.0",
    "lucide": "^0.479.0",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.37.1",
    "autoprefixer": "^10.4.21",
    "tailwindcss": "^4.1.14",
    "wrangler": "^4.92.0"
  }
}
